# Build output
/build/
/.gradle/
/*.jar
# 预构建产物，随仓库分发（便于直接取用）
!/xpfind.jar


# IDE
/.idea/
*.iml

# Machine-specific SDK path — never commit (Android convention)
/local.properties

# Target sample dex：第三方 SDK 的专有二进制，不随仓库分发（测试 fixture，本地自备）
/audience_network.dex
